# Same credentials as the Traeger mobile app.
# Loaded automatically when a .env file exists in the working directory,
# or set these in the environment (e.g. docker compose env_file).
#
# If the password contains # $ or spaces, use double quotes, e.g.:
# WIFIRE_PASSWORD="pass#word"
WIFIRE_USERNAME=
WIFIRE_PASSWORD=
