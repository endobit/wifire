name: build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: endobit/ci/.github/workflows/go-build.yaml@main
    with:
      go-experiment: jsonv2
      path: ./...
  lint:
    uses: endobit/ci/.github/workflows/go-lint.yaml@main
    with:
      go-experiment: jsonv2
